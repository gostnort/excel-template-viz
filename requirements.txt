nicegui>=2.0

pandas>=2.0

polars>=0.20

openpyxl>=3.1

gspread>=6.0

google-auth>=2.0

google-auth-oauthlib>=1.2

PyYAML>=6.0

tomlkit>=0.13

Pillow>=10.0

# LLM dependencies - llama.cpp for GGUF inference (CPU wheel)
# Windows CPU wheel compatibility (install.bat auto-detects via app/services/cpu_features.py):
#   0.3.28  - CPUs WITHOUT AVX512 (Ryzen 5600X, Core 7 150U, most laptops)
#   0.3.29+ - CPUs WITH AVX512F (Zen 4+, Xeon/workstation); 0xc000001d on non-AVX512
# Manual install (uses CPU detection):
#   python -c "from app.services.cpu_features import llama_cpp_install_command; print(llama_cpp_install_command())"
# Full table: QUICKSTART.md § LLM Dependencies
# install.bat pins the correct version before pip install -r requirements.txt
llama-cpp-python==0.3.28
huggingface-hub>=1.2.0,<2.0.0
psutil>=5.9.0

