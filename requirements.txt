nicegui>=2.0

pandas>=2.0

polars>=0.20

openpyxl>=3.1

gspread>=6.0

google-auth>=2.0

google-auth-oauthlib>=1.2

PyYAML>=6.0

tomlkit>=0.13

Pillow>=10.0

# llm_gemma4 (Gemma 4 E4B, docs/embed_gemma4.md): pulls in litert-lm-api==0.14.0 +
# litert-lm-builder==0.14.0. Pinned exactly -- 0.13.x lacks max_output_tokens on
# Conversation.send_message() and several other APIs this platform relies on.
litert-lm==0.14.0
huggingface-hub>=1.2.0,<2.0.0
psutil>=5.9.0

